# =====================================================================
# Comfyui-wwdm-tool 依赖清单
# ---------------------------------------------------------------------
# 本插件的运行时依赖已由 ComfyUI 主程序提供（见下方「ComfyUI 核心依赖」），
# 插件自身不引入任何额外的第三方包。
#
# 安装方式（ComfyUI 嵌入式环境）:
#     ..\..\..\venv\Scripts\python.exe -m pip install -r requirements.txt
# 或在 ComfyUI 根目录（存在 requirements.txt 的完整安装）直接:
#     pip install -r requirements.txt
# =====================================================================

# ---------------------------------------------------------------------
# ComfyUI 核心依赖（本插件正常运行所需，通常已随 ComfyUI 安装）
# ---------------------------------------------------------------------
torch>=2.1.0
torchaudio>=2.1.0
numpy>=1.24.0
Pillow>=9.0.0
aiohttp>=3.8.0

# ---------------------------------------------------------------------
# 说明
# ---------------------------------------------------------------------
# - torch / torchaudio : 音频张量（waveform）处理与采样率相关操作
# - numpy             : 波形峰值计算 / 可视化渲染
# - Pillow            : 波形预览图 PNG 渲染
# - aiohttp           : ComfyUI 服务端路由（/wwdm/audio/waveform）依赖
#
# 如需完整安装 ComfyUI 本体依赖，请使用 ComfyUI 根目录自带的
# requirements.txt（本文件仅覆盖本插件用到的子集）。
